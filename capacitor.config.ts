import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.idlelegend.game',
  appName: 'Idle Legend Bang Bang RPG',
  webDir: 'dist'
};

export default config;
